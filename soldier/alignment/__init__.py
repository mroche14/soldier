"""Alignment engine: rules, scenarios, context extraction, response generation.

The alignment engine is the brain of Soldier. It processes user turns through
a configurable pipeline that extracts context, retrieves relevant rules and
scenarios, generates responses, and enforces constraints.
"""
