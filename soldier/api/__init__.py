"""HTTP/gRPC interfaces: routes, middleware, request/response models.

The API layer exposes Soldier functionality via REST and gRPC endpoints,
handling authentication, rate limiting, and request routing.
"""
